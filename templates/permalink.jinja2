{% extends "base.jinja2" %}
{% block body %}
     <article class="entry" id="{{ entry.id }}">
        <h2>{{ entry.title }}</h3>
        <p class="dateline">{{ entry.created.strftime('%b. %d, %Y') }}</p>
        <hr/>
        <div class="entry_body">
            {{ entry.text|safe }}
        </div>
    </article>
{% endblock %}